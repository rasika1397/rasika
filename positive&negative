#include<stdio.h>
void main()
{
int num;
printf("\n enter the number: ");
scanf("%d",&num);
if(num>0)
{
printf("\n the number is positive");
}
else if(num<0)
{
printf("\n the number is negative");
}
else
{
printf("\n the number is zero");
}
getch();
}
